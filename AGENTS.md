## はじめに

- **本来の目的を見失わない**こと。特に失敗が続くと、目の前の問題解消に目的が移りがち。本来の目的が最も大事である。「木を見て森を見ず」「手段と目的の混同」を禁ずる。
- 指摘に対する過剰最適化はNG。言われた指摘を呪文に様に唱え、それだけに視線が向くことを禁ずる。

## Project

- セッション開始時に、プロジェクトルートの`README.md`を参照する。基本的なPJ概要と開発ルールを記載している。

## 規約

### コーディング

- **「長期的な保守性」「コードの美しさ」「堅牢性」** を担保する
  - SOLID、KISS、YAGNI、DRY
  - 責務の分離: ビジネスロジック、UI、データアクセスなどを適切に分離
  - 可読性と美しさ: 意味のまとまりごとに構造化、重複排除 など
- **局所最適・過剰設計はNG**。シンプルさと全体最適を意識する。不必要なフォールバック、念のためだけで意味のない保守的なコードなどは不要。
- 場当たり的な対応は禁止（バグフォールバック、ビルド/テスト通過のためだけの本質的でない修正）。目の前の問題を解決できても負債を残したら意味がないどころかマイナス。
- 「後方互換」は負債。既存利用維持のための互換分岐や旧仕様フォールバックは追加しない。新仕様へ一直線に置き換える。例として、「機能を消したこと」に対するテストは絶対に不要。
- ゴミコードの完全排除: デッドコード（関数、変数、型）、本質的な意味を持たないテストは絶対に排除する。`#[allow(dead_code)]`などで隠すことは最も最悪。LINTで出ないゴミコード（テストからしか呼ばれていないものなど）も排除。
- 公開範囲は最小化する。新規APIは`private`から始め、必要に応じて `pub(crate)` / `pub(super)` を選択する。
- Test: 
  - AAA パターンで読みやすく
  - テスト観点は最小に。重複した観点を無駄に増やさない。追加する場合は既存ケースの追加でできないか確認する
- その他ルール
  - Logging: `tracing` を使用、機密情報禁止
  - Error: `thiserror` で構造化、`Display` は lower-case
  - Doc: public item に doc comment、`# Errors` / `# Panics` / `# Safety`
  - Commit: Conventional Commits
  - PR: ドラフトにしない
  - 現状の変更差分を確認せずにgit checkoutで履歴を戻すことは禁止。ユーザーが意図的に残している差分がある可能性があるため。

### ドキュメント

- 文書を書く際は、視野を広く、大局観を意識。あとから見て違和感なく読めるように。
- 文書作成時はどんな時でも **MECE** を意識する（セクション構成、要件定義、設計書、PR詳細などすべて）
- 実装後、関連内容が `docs/` にある場合は反映する。ただし「差分」を書くのではない。反映するのは明確な記載誤りの修正か、本当に記載するべき内容のみである。
- **メタ記述は一切書かない(重要)**: ユーザーに指摘された修正理由、単なる差分、過去の状態、進行管理上の名称(P0、P1、phase2、MVPなど)などは絶対に残してはならない。残すのは永続化すべき事実だけであり、。コード、コードコメント、コミットメッセージも同じ。絶対に書いてはならない。
- ドキュメントには「勾配」を付けることを意識する。勾配の無いドキュメントはただのデータである。記載する前に、まず文章の流れや章建て等の全体を確認する。決して直近だけ見て差分を挿入するというやり方にしてはならない。

### Review

- 「自己レビュー」とはなにか: 
  - **目的**: 自分が持つコンテキストを活かし、他者レビューの前に、敵対的視点でレビューし実装不正をすべて潰すこと。実装者だからこそ効率よく発見できる類の欠陥（意図からの乖離・可視性の過剰・デッドコード・テストの assert 実効性・docs 整合）を、自分の手で撲滅する。
  - 証跡（チェックリスト・行番号・検証コマンドなど）は目的を達成する手段であり、リストを埋めることが完了ではない。
  - 本物のレビューなら、コンテキストを持つ者が実装を見直せばほぼ必ず何か見つかる。「何も見つからなかった」場合は未レビュー扱いと考える。

## さいごに

- 短期的視点での解決より長期的視点での解決
- 迷ったら一歩下がって俯瞰的・メタ的に思考する
